<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="assets/css/style.css"/>
</head>
<body>
    <% plugins.forEach(function(item){ %>
        <%- item.postion %>
        <% if (item.position == 'top_menu') { %>
            <%- item.content %>
        <% } %>
    <%  }); %>
